// Placeholder until Firebase is configured.
//
// Run `flutterfire configure` in the project root to replace this file with
// your real Firebase project settings. While this placeholder is in place the
// app runs in offline demo mode (accounts and scores stay on the device).
import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase is not configured yet. Run `flutterfire configure`.',
    );
  }
}
