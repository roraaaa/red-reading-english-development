/// Addresses that open outside the app.
abstract final class AppLinks {
  /// Where `docs/index.html` is published.
  ///
  /// GitHub Pages serves the `docs/` folder once it is switched on in the
  /// repository's Settings -> Pages (the repository has to be public, or on a
  /// paid plan). Keep this in step with the privacy policy URL in the Play
  /// Console and App Store Connect listings.
  static const privacyPolicy = 'https://roraaaa.github.io/red-reading-english-development/';
}
